#pragma once
#include <vector>
#include <fstream>
/*
using namespace std;


enum token;

class tokenstring
{
public:
	tokenstring();
	tokenstring(ifstream);

	token cur();
	int next();
	int add(token);



private:
	vector<token> tokens;
	int index;

	
};
*/